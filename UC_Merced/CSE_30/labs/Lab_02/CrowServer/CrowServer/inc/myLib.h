#ifndef MYLIB
#define MYLIB

#include <string>
#include <server.h>

std::string sayHello();

std::string echoWord(std::string);

ucm::json getSuperBowlScore();

std::string upperCase(std::string);

#endif
